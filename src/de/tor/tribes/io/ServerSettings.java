/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package de.tor.tribes.io;

import de.tor.tribes.util.GlobalOptions;
import java.net.URL;

/**
 *
 * @author Jejkal
 */
public class ServerSettings {

    public static void loadUnitSettings() throws Exception {
    /*    String serverID = GlobalOptions.getSelectedServer();
        if (serverID == null) {
            throw new Exception("Es wurde kein Server ausgewählt");
        }
        try {
            String unitURLString = ServerList.getServerURL(serverID).toString();
            unitURLString += "/interface.php?func=get_unit_info";
            URL unitURL = new URL(unitURLString);
            InputStream unitStream = unitURL.openConnection().getInputStream();
            byte[] data = new byte[1024];
            File tmpFile = 
        } catch (Exception e) {
            throw new Exception("Fehler beim laden der Einheiteninformationen");
        }*/

    }
}
